/**
 * Exploration-only presentation entry point. The implementation is kept
 * backward-compatible with the original data-source component path while
 * all new navigation imports it from the data-exploration feature.
 */
export { default } from '@/pages/data-source/components/DataExplorationDrawer';
