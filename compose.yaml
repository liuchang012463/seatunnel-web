services:
  mysql:
    image: mysql:8.0.39
    container_name: seatunnel-web-mysql
    restart: unless-stopped

    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-change_me}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-seatunnel_web}
      MYSQL_USER: ${MYSQL_USER:-seatunnel}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-change_me}
      TZ: Asia/Shanghai

    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
      - --default-time-zone=+08:00

    ports:
      - "${MYSQL_HOST_PORT:-3306}:3306"

    volumes:
      - mysql-data:/var/lib/mysql

    healthcheck:
      test:
        - CMD-SHELL
        - 'MYSQL_PWD="$${MYSQL_ROOT_PASSWORD}" mysqladmin ping -h 127.0.0.1 -uroot --silent'
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

    networks:
      - seatunnel-web-network


  seatunnel-web-api:
    image: ${SEATUNNEL_WEB_API_IMAGE:-seatunnel-web-api:latest}
    container_name: seatunnel-web-api
    restart: unless-stopped

    build:
      context: .
      dockerfile: Dockerfile
      target: backend-runtime

    depends_on:
      mysql:
        condition: service_healthy

    environment:
      TZ: Asia/Shanghai

      JAVA_OPTS: ${JAVA_OPTS:--Xms512m -Xmx1024m}

      APP_OPTS: ${APP_OPTS:-}

      SPRING_AI_API_KEY: ${SPRING_AI_API_KEY:-}
      SPRING_AI_BASE_URL: ${SPRING_AI_BASE_URL:-}
      SPRING_AI_LLM_MODEL: ${SPRING_AI_LLM_MODEL:-}

      SERVER_PORT: 9527

      SPRING_PROFILES_ACTIVE: mysql

      SEATUNNEL_WEB_DATABASE_TYPE: mysql
      SEATUNNEL_WEB_DATABASE_HOST: mysql
      SEATUNNEL_WEB_DATABASE_PORT: 3306
      SEATUNNEL_WEB_DATABASE_NAME: ${MYSQL_DATABASE:-seatunnel_web}

      SPRING_DATASOURCE_URL: "jdbc:mysql://mysql:3306/${MYSQL_DATABASE:-seatunnel_web}?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=GMT%2B8&allowPublicKeyRetrieval=true"
      SPRING_DATASOURCE_USERNAME: ${MYSQL_USER:-seatunnel}
      SPRING_DATASOURCE_PASSWORD: ${MYSQL_PASSWORD:-change_me}

      SPRING_FLYWAY_ENABLED: "true"

      SEATUNNEL_WEB_DATASOURCE_MASTER_KEY: ${SEATUNNEL_WEB_DATASOURCE_MASTER_KEY:?Please set SEATUNNEL_WEB_DATASOURCE_MASTER_KEY in .env}

      METADATA_OPENMETADATA_ENABLED: ${METADATA_OPENMETADATA_ENABLED:-false}
      METADATA_OPENMETADATA_BASE_URL: ${METADATA_OPENMETADATA_BASE_URL:-}
      METADATA_OPENMETADATA_TOKEN: ${METADATA_OPENMETADATA_TOKEN:-}
      METADATA_OPENMETADATA_KINGBASE_TUNNEL_HOST: ${METADATA_OPENMETADATA_KINGBASE_TUNNEL_HOST:-}
      METADATA_OPENMETADATA_KINGBASE_TUNNEL_PORT: ${METADATA_OPENMETADATA_KINGBASE_TUNNEL_PORT:-0}
      METADATA_OPENMETADATA_EXTENSION_ENABLED: ${METADATA_OPENMETADATA_EXTENSION_ENABLED:-false}
      METADATA_OPENMETADATA_EXTENSION_BASE_URL: ${METADATA_OPENMETADATA_EXTENSION_BASE_URL:-}

    extra_hosts:
      - "host.docker.internal:host-gateway"

    expose:
      - "9527"

    volumes:
      - seatunnel-web-logs:/opt/seatunnel-web/logs
      - seatunnel-web-jdbc-drivers:/opt/seatunnel-web/jdbc-drivers

    healthcheck:
      test:
        - CMD-SHELL
        - 'bash -c "</dev/tcp/127.0.0.1/9527"'
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s

    networks:
      - seatunnel-web-network


  seatunnel-web:
    image: ${SEATUNNEL_WEB_IMAGE:-seatunnel-web:latest}
    container_name: seatunnel-web
    restart: unless-stopped

    build:
      context: .
      dockerfile: Dockerfile
      target: frontend-runtime

    healthcheck:
      disable: true

    depends_on:
      seatunnel-web-api:
        condition: service_healthy

    ports:
      - "${SEATUNNEL_WEB_PORT:-9527}:80"

    networks:
      - seatunnel-web-network


volumes:
  mysql-data:
    name: seatunnel-web-mysql-data

  seatunnel-web-logs:
    name: seatunnel-web-logs

  seatunnel-web-jdbc-drivers:
    name: seatunnel-web-jdbc-drivers


networks:
  seatunnel-web-network:
    name: seatunnel-web-network
    driver: bridge
