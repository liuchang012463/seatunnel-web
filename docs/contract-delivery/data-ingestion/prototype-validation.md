# 数据采集引接原型验收记录

## 1. 自动校验

| 校验项 | 命令 | 结果 |
|---|---|---|
| 指标数据生成与完整性 | `npm run prototype:generate` | 55 条原子指标、20 个页面通过 |
| 页面注册表与 Mock 请求测试 | `npm run jest -- --runInBand src/prototype/__tests__/registry.test.ts src/prototype/__tests__/mockTransport.test.ts` | 通过 |
| 正常模式生产构建 | `npm run build` | 通过 |
| 原型模式生产构建 | 设置 `REACT_APP_PROTOTYPE=1`、`UMI_APP_PROTOTYPE=1` 后执行 `npm run build` | 通过 |

全量 `npm run tsc` 当前会命中存量项目中既有的类型错误。交付验收以变更文件错误过滤、Jest 和两种生产构建为准；未在本次菜单原型任务中扩大范围修复既有类型债务。

## 2. 浏览器点击验收

浏览器验收使用原型生产构建和纯前端 Mock 运行时完成。

| 范围 | 页面数 | 验收内容 | 结果 |
|---|---:|---|---|
| 数据采报 | 2 | 创建发布模板、模拟填报、生成报告、预览导出 | 通过 |
| 引接资源 | 3 | 数据源、引擎状态动作、探查页签和扫描 | 通过 |
| 数据引接 | 6 | 离线/实时状态、云边断网恢复、边缘连通、链路详情、拓扑高亮 | 通过 |
| 运行运维 | 4 | 态势、监控、告警状态动作与故障证据抽屉 | 通过 |
| 入湖管理 | 3 | 资源连接测试、生命周期执行、逻辑查询预览 | 通过 |
| 系统管理 | 2 | 参数知识与开放接口状态动作 | 通过 |

关系图额外验证：

- 默认展示 20 个页面节点以及 `F-01～F-15`、`P-01～P-06`。
- 点击父指标能够展开原子指标。
- 点击页面节点能够进入对应业务页面。
- 页面顶部能够返回关系图并显示复用、新增、改造、集成或有限实现标注。
- 关键操作执行后 URL 保持在对应页面，且产生状态变化、详情或消息反馈。

## 3. 外部依赖说明

本次浏览器验收没有连接 SeaTunnel 后端、TDuck、OpenMetadata 或 SSO。真实接口、数据同步、身份映射和性能验收属于后续模块设计与开发阶段。
