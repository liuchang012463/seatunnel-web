package org.apache.seatunnel.web.api.lake.recommendation;

/** Server recommendation for the requested lake usage shape. */
public enum LakeRecommendationMode {
    PHYSICAL,
    LOGICAL,
    UNSUPPORTED
}
