# SeaTunnel Web UI Design Baseline

> 本文件是 SeaTunnel Web 的项目级 UI 设计基线。它定义长期稳定的页面结构、视觉语义、交互不变量和验收要求；数据探查工作区是当前最完整的参考实现。它不是设计稿坐标，也不是某一次改版的验证日志。

## 1. Purpose & Scope

### 1.1 适用范围

- 所有新增页面、现有页面重构和跨页面组件；优先复用本文件的语义 token、布局契约和状态模型。
- 当前已落地并作为参考实现的数据探查页面：
  - `/data-exploration/overview`：概览和覆盖率。
  - `/data-exploration/tasks`：一次性探查任务配置。
  - `/data-exploration/results`：按范围和类型浏览探查结果。
  - `/data-exploration/results?dataSourceId=...&dbType=...`：数据源详情工作区。

### 1.2 文档与代码的职责

| 层级 | 事实来源 | 负责内容 |
| --- | --- | --- |
| 产品/交互契约 | `DESIGN.md` | 页面职责、状态转换、URL 状态、响应式约束和不变量 |
| 可执行视觉 token | `seatunnel-web-ui/src/design-system.less` | 颜色、字体、字号、控件高度、圆角和全局组件覆盖 |
| 参考资料 | `docs/样式参考/design-spec.json`、`docs/样式参考/样式参考图1.png`、`docs/样式参考/样式参考图2.png` | 设计来源和视觉依据，不直接替代运行时 token |
| 页面实现 | `seatunnel-web-ui/src/pages/` | 将契约映射为组件、数据请求和局部布局 |

设计参考图只用于理解层级、密度和视觉方向。长期文档不得依赖 `/tmp`、本机临时截图或一次性浏览器产物；如果截图需要成为团队共同资产，应复制到仓库内明确的 `docs/` 目录并说明用途。

### 1.3 边界

本文件可以规定视觉和交互行为，例如“单位改变后清空依赖筛选”和“探查任务不是定时调度器”。它不替代产品需求、权限说明、API 契约、指标口径或连接器协议文档；这些内容应在对应领域文档中维护。

## 2. Design Principles

1. **数据优先，层级清晰。** 深色工作台用于承载高密度元数据，但标题、关键数值和主操作必须有明确层级。
2. **一个页面一个主任务。** 概览负责理解范围，任务负责发起一次性探查，结果负责筛选和定位，详情负责读取资源；不要在同一页面隐式增加另一套工作流。
3. **保持上下文。** 进入详情、切换目录或刷新异步数据时，用户的筛选范围、当前位置和当前目标不能无故丢失。
4. **按能力建模。** UI 根据资源能力选择工作区模式，不根据 connector name 堆叠条件分支。
5. **异步状态可解释。** 加载、失败、空结果、不可用和成功都必须有稳定的页面结构和下一步提示。
6. **语义 token 优先。** 页面样式引用 `--st-*` token；新页面不能重新创建同义的颜色、字体或状态体系。
7. **本地化优先。** 用户可见文案使用 locale key；协议名、连接器名和 SQL 类型等官方名称可以保留原文。英文眉标题只能作为视觉层，不替代本地化标签和无障碍名称。

### 2.1 全局不变量

- 主浏览动作 MUST NOT 依赖右侧 Inspector；Inspector 隐藏时，主任务仍然可完成。
- 任何依赖筛选发生变化时，所有下游选择 MUST 被清空，不能展示与新范围不一致的旧结果。
- 目录切换时，旧资源选择 MUST 在新目录数据渲染前失效，不能短暂显示过期详情。
- 异步请求失败时 MUST 保留页面骨架、当前筛选和可重试入口。
- 状态不能只用颜色表达；状态标签、图标或文本至少还要提供一种可感知线索。
- 新增页面 MUST 使用项目级 token，不得把设计稿中的坐标、局部颜色或临时截图路径写入实现。

## 3. Information Architecture

| 页面 | 用户问题 | 主动作 | 不负责的事情 |
| --- | --- | --- | --- |
| 概览 | 有多少数据，覆盖到什么程度？ | 选择范围、查看指标、进入结果 | 配置探查细节 |
| 任务 | 对哪些数据源发起一次探查？ | 选择资源、开始一次性探查、查看状态 | 创建定时调度 |
| 结果 | 当前范围有哪些探查结果？ | 类型筛选、范围筛选、搜索、进入详情 | 替代资源详情中的深度浏览 |
| 详情 | 选中的数据源有哪些结构和画像？ | 浏览目录、查看详情、读取样本或指标 | 承担结果页的全局筛选 |

结果页和详情页共享浏览上下文，但职责不同：结果页定位数据源，详情工作区定位资源。详情关闭或返回时只移除详情状态，保留结果页筛选。

## 4. Workspace Model

### 4.1 统一工作区骨架

数据探查结果和详情优先使用以下骨架：

| 区域 | 责任 | 约束 |
| --- | --- | --- |
| 顶部标题区 | 当前页面/资源标题、范围、主操作 | 标题和主操作在窄屏可换行，不得挤压主内容 |
| 左侧目录/类型栏 | 类型、层级或目录导航 | 只负责定位，不承载主详情 |
| 中心内容区 | 当前选择的资源、列表、表格、样本或画像 | 是主浏览区，必须有加载、失败、空和成功态 |
| 右侧 Inspector | 当前范围、统计、标签、连接器和状态 | 只提供上下文，不承载唯一主操作 |

### 4.2 页面结构

**概览**

页面顺序为：页面标题 → 范围筛选 → 元数据规模 → 覆盖率与当前范围 → 数据源探查状态。指标必须说明当前筛选范围；百分比同时提供分子/分母或绝对值。

**任务配置**

搜索和筛选位于同一工具条，数据源状态表紧随其后。“重新扫描”“开始探查”“运行记录”“查看结果”是行级动作。切换数据源时清空上一次 Database 选择；没有可用 Database 时不能提交。

**结果**

结果工作区由 `EXPLORATION TYPES`、`METADATA RESULTS` 和 `INSPECTOR` 三个区域组成。资源卡片至少提供名称、类型、环境、归属、探查状态和明确的详情入口；类型、搜索和范围筛选可以叠加。

**详情**

数据库、消息/API 资源和文件资源复用统一骨架，中心内容由能力模式决定。右侧 Inspector 是上下文补充，不得成为目录导航或主浏览的唯一入口。

## 5. Connector Capability Model

连接器必须先映射到能力模式，再决定详情工作区的布局。概念模型如下，具体类型名可以在实现中调整：

```ts
type ExplorationMode =
  | 'database'
  | 'resource-list'
  | 'hierarchical-files';

interface ExplorationCapabilities {
  mode: ExplorationMode;
  supportsSampleData: boolean;
  supportsStatistics: boolean;
  supportsSearch: boolean;
}
```

| 能力模式 | 典型连接器 | 中心内容 | 可选能力 |
| --- | --- | --- | --- |
| `database` | JDBC、Doris、Elasticsearch 等 | Database → Schema → Table，表结构和详情 | 样本数据、统计画像 |
| `resource-list` | Kafka、HTTP/API 等 | 一级资源列表、资源概览和属性 | 搜索、样本或协议特有预览 |
| `hierarchical-files` | MinIO、S3、FTP、SFTP | 面包屑、目录和文件/对象详情 | 预览、文件属性、搜索 |

约束：

- connector registry 负责声明类型、分组和能力；页面只消费能力，不直接判断一长串连接器名称。
- `supportsSampleData` 或 `supportsStatistics` 为 false 时，相关区域应隐藏或明确说明“不支持”，不能渲染空白面板。
- 新增连接器必须说明它属于哪种模式、支持哪些能力以及不支持时的替代文案。
- 层级资源导航必须提供面包屑或等价的返回上级入口，并在切换父级后清除失效的子级选择。

## 6. Interaction Contract

### 6.1 筛选和依赖关系

- 单位改变：清空业务系统、数据源详情和依赖统计；重新加载业务系统选项。
- 业务系统改变：清空数据源详情和依赖统计，保留单位。
- 类型改变：清空当前详情选择，保留单位和业务系统范围。
- 搜索只作用于当前已加载的结果列表，不改变服务端范围统计。
- 禁用的依赖筛选必须解释原因，例如未选择单位时业务系统显示“先选择单位”。

### 6.2 URL State

URL 保存可分享、可恢复的导航状态；瞬时 UI 状态保留在组件状态中。

| 状态 | URL 参数 | 是否持久化 | 规则 |
| --- | --- | --- | --- |
| 单位范围 | `unitId` | 是 | 刷新后恢复；清空单位时同步移除业务系统 |
| 业务系统范围 | `businessSystemId` | 是 | 仅在单位范围有效；刷新后恢复 |
| 当前数据源 | `dataSourceId` | 是 | 仅详情态存在；刷新详情页应尝试恢复 |
| 连接器类型 | `dbType` | 是 | 与 `dataSourceId` 一起作为详情路由状态，不是独立筛选 |
| 类型分组 | 当前组件状态 | 否 | 关闭/返回详情时清空当前详情选择 |
| 搜索词 | 当前组件状态 | 否 | 除非产品明确需要可分享搜索，否则不写入 URL |

导航不变量：

- 详情关闭/返回结果页时，只移除 `dataSourceId` 和 `dbType`，保留 `unitId`、`businessSystemId` 以及仍然有效的列表筛选。
- 浏览器刷新、Back 和 Forward 后，界面必须与 URL 表达的列表/详情状态一致；路由实现不得让组件内部旧状态覆盖 URL。
- 无效或已删除的 `dataSourceId` 不能阻塞结果页；显示可解释的失败/空态，并允许回到保留筛选的结果页。

### 6.3 加载、失败、空和成功

| 状态 | 页面要求 |
| --- | --- |
| 加载中 | 保留标题、筛选和区域骨架；主内容显示明确 loading，不用空态冒充加载完成 |
| 失败 | 保留当前上下文，说明失败范围，提供重试；不能静默清空已有可用数据 |
| 空结果 | 说明为什么为空以及下一步，例如调整筛选或先完成探查 |
| 未选择 | 说明如何从左侧目录选择资源；不要只放空图标 |
| 成功 | 展示当前范围、更新时间或状态来源，并让主动作可见 |
| 不支持 | 说明连接器不提供该能力；不渲染无标题、无解释的空面板 |

### 6.4 选择清理

- 任何父级筛选、目录或连接器发生变化时，依赖选择先失效，再发起下一次加载。
- 新目录返回前不显示旧目录的资源详情。
- 详情页的返回操作不应清除结果页的单位和业务系统范围。
- 重新加载不应重置用户未改变的筛选和目录位置，除非服务端明确返回资源已不存在。

## 7. Visual System

### 7.1 Color tokens

以下名称是语义契约，实际值统一维护在 `seatunnel-web-ui/src/design-system.less`。即使两个 token 当前数值相同，也不能互相替代：`primary` 表示操作强调，`border` 表示结构边界。

| 语义 | CSS token | 当前基准值 | 用途 |
| --- | --- | --- | --- |
| 应用/页面底色 | `--st-color-bg-app` / `--st-color-bg-page` | `#001922` | shell 和页面背景 |
| 主面板 | `--st-color-bg-panel` | `#002E41` | 卡片、工作区、表格容器 |
| 提升层 | `--st-color-bg-elevated` | `#07394A` | 弹层、浮层、提升内容 |
| 控件底色 | `--st-color-bg-control` | `rgba(0,25,34,.72)` | 输入框、选择器、资源条目 |
| 交互底色 | `--st-color-bg-interactive` | `#0A526A` | 可交互的强调表面 |
| 主文字 | `--st-color-text-primary` | `#FFFFFF` | 页面标题、资源名、关键数据 |
| 次文字 | `--st-color-text-secondary` | `#D5D5D5` | 正文、表格内容、重要说明 |
| 支持文字 | `--st-color-text-supporting` | `#8799A3` | 有意义的辅助说明、元数据 |
| 装饰弱文字 | `--st-color-text-muted` | `#666F75` | 仅用于非必要眉标题、装饰性 caption、占位信息 |
| 强调色 | `--st-color-accent` | `#4DD2FF` | 链接、图标、进度、焦点和主强调 |
| 操作主色 | `--st-color-primary` | `#2187A8` | 主按钮和操作强调 |
| 操作激活色 | `--st-color-primary-active` | `#117DA0` | pressed/active 状态 |
| 强结构边框 | `--st-color-border-strong` | `#2187A8` | 控件和容器的明确边界 |
| 通用边框 | `--st-color-border` | 当前同强结构边框 | 兼容现有页面；新代码不得改用 `primary` 表达边框 |
| 普通分隔线 | `--st-color-divider` | `rgba(17,125,160,.24)` | 区域分隔和表头边界 |
| 弱分隔线 | `--st-color-divider-subtle` | `rgba(17,125,160,.1137)` | 次级层级 |
| 悬停 | `--st-color-hover` | `rgba(33,135,168,.24)` | hover 背景 |
| 选中 | `--st-color-selected` | `rgba(33,135,168,.42)` | 当前导航、当前资源、当前类型 |
| 焦点 | `--st-color-focus` | `rgba(77,210,255,.2)` | focus 背景或 focus ring 辅助层 |

规则：

- 页面样式不得硬编码新的同义颜色；临时颜色必须有明确的组件语义并在评审中说明。
- 有意义的正文和辅助说明不得使用 `--st-color-text-muted`；使用 supporting 或 secondary token，并在深色背景上检查可读性。
- 现有页面中把 `--st-color-text-muted` 用作普通辅助文字的旧用法属于迁移债务；修改该区域时应迁移到 supporting 或 secondary，不要继续复制旧模式。
- 对比度不足的文字不得通过加粗或阴影掩盖；应提升语义 token 或调整背景。
- 边框、分隔线和按钮填充分别使用 border/divider/primary token，即使当前颜色值相同也保持语义独立。

### 7.2 Typography

字体族由 `--st-font-family` 统一提供，页面不得单独决定字体。当前 fallback 顺序兼顾 Windows 中文字体、CJK 字体和 macOS/Linux 系统字体。

| 层级 | 字号 / 行高 | 典型使用 |
| --- | --- | --- |
| large title | `28px / 37px` | 页面级标题、概览主标题 |
| title | `24px / 31px` | 详情资源标题 |
| title/content | `20px / 26px` | 面板标题、弹层标题 |
| content-lg | `18px / 24px` | 重要内容和数据 |
| content-md | `16px / 21px` | 默认内容 |
| content-sm | `14px / 19px` | 表格、控件和辅助内容 |
| caption | `12px / 16px` | 状态、说明和装饰性眉标题 |

标题使用常规字重，通过字号、颜色和留白建立层级。大写英文眉标题如 `METADATA RESULTS`、`INSPECTOR` 只用于已有工作台视觉语言；对应的可访问名称和业务文案仍然使用 locale key。

### 7.3 Controls & states

- 标准控件视觉高度 `34px`，大控件视觉高度 `38px`；这是视觉密度约束，不等于可点击区域。
- 触摸或窄屏场景的可点击区域最小 `44px`；可以通过外层 padding、行高或透明 hit area 达成，不强行放大视觉控件。
- icon-only 控件必须有 accessible name、可见 focus 状态和不依赖颜色的 hover/active 反馈。
- 基础圆角使用 `--st-radius-sm` / `--st-radius-md`；标签可以使用胶囊形，容器不使用大面积圆角和重阴影。
- `:focus-visible` 必须提供清晰 outline，不能只改变背景色；禁用状态要同时降低交互能力和视觉强调。
- loading、processing、success、warning、error 需要文本或图标辅助，不能只使用红/绿/蓝色区分。

## 8. Responsive Layout

响应式规则描述可执行的约束，而不是只描述“适配移动端”。结果工作区的当前约束如下；新页面可以采用不同尺寸，但必须明确自己的最小宽度、列约束和滚动所有者。

| 视口 | 布局约束 | 滚动和交互 |
| --- | --- | --- |
| `≥ 1381px` | 三栏；左栏 `290–300px`，中心 `minmax(520px, 1fr)`，右栏 `270–286px` | 工作区固定在视口剩余高度；中心列表和 Inspector body 独立纵向滚动 |
| `1161–1380px` | 三栏；左栏约 `290px`，中心保持 `minmax(480px, 1fr)`，右栏约 `270px` | 保留 Inspector；长标题和操作区允许换行 |
| `901–1160px` | 三栏；左栏 `276px`，中心 `minmax(480px, 1fr)`，右栏 `258px` | 顶部工具区纵向排列；不得产生横向页面滚动 |
| `681–900px` | 两栏；左栏 `250px`，中心 `minmax(0, 1fr)`；隐藏 Inspector | 隐藏 Inspector 不得隐藏主浏览、搜索、返回和选择动作 |
| `≤ 680px` | 单列；筛选器和操作按钮占满宽度；目录区域最大约 `330px`，中心区域最小约 `620px` | 页面纵向滚动；资源列表自己处理长列表；目录、按钮和输入的 hit area 仍满足触摸要求 |

补充约束：

- 中心列必须允许 `minmax(0, 1fr)`，长名称换行，不以固定宽度撑破页面。
- 桌面工作区由中心列表和 Inspector body 承担内部滚动；移动端优先让页面纵向滚动，不制造双向滚动陷阱。
- 隐藏次要信息时，必须保留同等主任务的替代入口；不能把“Inspector 隐藏”实现成“详情不可用”。
- 每个新页面应至少定义一个桌面宽度、一个 Inspector/侧栏隐藏宽度和一个窄屏单列宽度的验收状态。

## 9. Implementation Boundaries

实现可以重命名组件或调整目录，但必须保留本文件的能力和交互契约。当前代码定位入口仅用于导航：

| 关注点 | 当前入口 |
| --- | --- |
| 全局 token 和 Ant Design 覆盖 | `seatunnel-web-ui/src/design-system.less`、`src/global.less` |
| 数据探查页面 | `seatunnel-web-ui/src/pages/data-exploration/` |
| 数据源类型和分组 | `seatunnel-web-ui/src/pages/data-source/dataSourceRegistry.ts` |
| 数据库/通用详情工作区 | `seatunnel-web-ui/src/pages/data-source/components/` |

实现规则：

- 页面局部样式只负责布局和组件组合，不复制全局 token 表。
- Connector registry 声明类型、分组和能力；页面消费 capability model。
- 通过语义状态选择 loading/error/empty/unsupported 的 UI，不通过 connector name 猜测结构。
- 文件名和组件名是实现细节；重构时先保留路由、URL、能力模式和状态不变量，再移动代码。
- 参考图中的绝对坐标、临时 mock 数据和浏览器截图不能成为运行时依赖。

## 10. Acceptance Criteria

新增或修改页面完成前，至少满足以下要求：

### Visual

- 所有颜色、字体、字号、控件高度和状态层级来自项目 token，或在评审中明确新增语义 token。
- 页面在桌面、Inspector 隐藏和窄屏单列三种宽度下没有横向溢出，主操作仍可完成。
- 页面密度与深色工作台一致：层级靠留白、分隔线和颜色建立，不依赖大面积白底、重阴影或随意圆角。

### Interaction

- 依赖筛选清理、目录切换清理和详情返回保留上下文符合第 6 节不变量。
- URL 中的持久状态可刷新恢复；详情参数移除时不误删列表筛选。
- loading、error、empty、unsupported、success 均有稳定结构和下一步提示。

### Accessibility

- 有意义的正文和辅助信息不使用装饰性 muted token。
- 键盘可以到达和操作主流程；focus-visible 清晰可见。
- icon-only 控件有可访问名称；状态不只依赖颜色；长文本可换行。
- 视觉高度和点击区域分开验收，窄屏/触摸场景的可点击区域达到最小要求。

### Verification

- 使用 Playwright 覆盖主流程：范围筛选、类型筛选、搜索、进入/退出详情、数据库层级导航、通用资源/文件导航、空态、失败重试和窄屏布局。
- 使用浏览器控制台检查本次改动没有新增 uncaught error；既有 warning 要单独记录，不把 warning 伪装成通过条件。
- 按变更范围运行 TypeScript、lint 和必要的构建检查；验证结果写在 PR 或提交说明中，不写入本长期基线。
- 需要视觉证据时，将截图作为 PR/评审产物；只有明确要长期维护的参考图才进入仓库 `docs/` 目录。

## 11. Reference Assets

- 视觉参考：`docs/样式参考/样式参考图1.png`、`docs/样式参考/样式参考图2.png`。
- 结构化视觉参数：`docs/样式参考/design-spec.json`。
- 可执行 token：`seatunnel-web-ui/src/design-system.less`。
- 本文件不记录某次运行的 Playwright 通过数量、具体浏览器截图目录、TypeScript 结果或控制台计数；这些属于变更验证记录。

当参考图、设计 token 和现有页面出现冲突时，优先级为：无障碍和交互不变量 → 本文件的语义契约 → 可执行 token → 页面局部实现 → 参考图的视觉细节。
